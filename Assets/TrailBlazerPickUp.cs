using UnityEngine;
using System.Collections;

public class TrailBlazerPickUp : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.Rotate(new Vector3(270, 0, 0));
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
