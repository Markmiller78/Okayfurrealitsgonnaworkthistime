using UnityEngine;
using System.Collections;

public class light_pulse : MonoBehaviour {

    float Intensity;
    float Range;
	// Use this for initialization
	void Start () {
	//Intensity = gameObject.GetComponent<Intensity
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
